# Vendored 3D assets — treat as binary so git never normalizes line endings,
# diffs them as text, or attempts a merge. Keeps the byte stream identical to
# what scripts/fetch-kaykit.mjs produced.
public/models/** binary
